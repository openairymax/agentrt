name: Quality Gates

# WS-6 6.3（0.1.15 门禁接线）：把伞仓 tools/scripts 下的 quality-gate 九门禁
# 编排器接入 agentrt 发布平面的 workflow（接线前 workflow 引用数 = 0）。
#
# 宿主迁移范式（同 codegen-check.yml 的 ssot-registry-validate）：门禁脚本与
# SSoT 数据位于伞仓 airymaxhub（tools/docs/agent-workload），故本作业以伞仓
# GitHub 镜像为数据源浅克隆到临时区、初始化子模块后运行 quality-gate.sh。
#
# 九门禁：1 编译 · 2 BAN 规则 · 3 安全扫描 · 4 合约版本 · 5 跨仓验证 ·
#          6 圈复杂度 · 7 SSoT 权威源 · 8 头文件重复度 · 9 coreloopthree ABI 冻结。
#
# 触发面瘦身（2026-09，缓"流水线太慢"）：纯文档变更跳过（安全边界同
# build-test.yml——只忽略 md/docs，源码与 workflow 变更仍全量验证）；同一
# ref 的并发运行自动取消；构建一律树外化到 runner temp，不落源码区。

on:
  push:
    branches: [main]
    paths-ignore:
      - '*.md'
      - '**/*.md'
      - 'docs/**'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Checkout (agentrt host, minimal)
        uses: actions/checkout@v4
        with:
          submodules: false
          fetch-depth: 1

      # Gate 1（编译）所需的 C 工具链与依赖库；Gate 6（圈复杂度）需 lizard。
      # Gate 3（安全扫描）外部工具（trivy/gitleaks/cppcheck）缺失时按 skip 处理，
      # 不构成硬依赖，故此处不安装，避免拉长流水线。
      - name: Install gate dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential cmake ninja-build \
            libsqlite3-dev libcjson-dev libyaml-dev libcurl4-openssl-dev \
            libssl-dev zlib1g-dev libzstd-dev \
            libmicrohttpd-dev libwebsockets-dev libevent-dev \
            python3 python3-pip
          python3 -m pip install --quiet 'lizard==1.23.0'

      # 以伞仓 GitHub 镜像为数据源（脚本=tools、registry=docs、宿主=agent-workload）。
      # 私有嵌套子模块（agentrt/atoms 等）经 GH_TOKEN（org PAT）~/.netrc 认证。
      - name: Clone umbrella dataset (docs, tools, agent-workload)
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          if [ -n "${GH_TOKEN:-}" ]; then
            printf 'machine github.com\nlogin x-access-token\npassword %s\n' \
              "$GH_TOKEN" > "$HOME/.netrc"
            chmod 600 "$HOME/.netrc"
          fi
          git clone -q --depth 1 \
            https://github.com/openairymax/airymaxhub.git "$RUNNER_TEMP/airymaxhub"
          git -C "$RUNNER_TEMP/airymaxhub" submodule update --init --recursive -- \
            docs tools agent-workload

      - name: Run quality gates (9 scripts)
        env:
          # 构建产物一律树外化（工程铁律 a）：落到 runner temp，不落源码区。
          QUALITY_GATE_BUILD_DIR: ${{ runner.temp }}/agentrt-quality-gate-build
        run: |
          bash "$RUNNER_TEMP/airymaxhub/tools/scripts/ci/quality/gates/quality-gate.sh"
