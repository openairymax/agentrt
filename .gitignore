# =============================================================================
# AgentRT .gitignore 文件
# 版本：12.1.0
# 最后更新：2026-06-19
# 说明：规范化排除所有不应纳入版本控制的文件和目录
#
# 设计原则：
# 1. 构建产物一律排除（CMake 缓存、编译产物等）
# 2. 运行时生成的文件一律排除（数据库、日志、模型等）
# 3. 本地开发配置一律排除（.env.local、IDE 配置等）
# 4. 空目录通过 .gitkeep 保留，但排除目录内容
# 5. 项目必需文件使用否定规则明确保留
# 6. 通配符规则限定作用范围，避免意外排除项目必需文件
#
# V12.0.0 更新（v0.1.1 发行准备 — 三智能体团队 + 合约优先）：
# - manager/ 和 openlab/ 已移至 ecosystem/ 子目录
# - toolkit/ 已移至 sdk/ 子目录
# - 新增 deploy/ 目录（Docker 容器化部署）
# - 新增 contracts/ 排除规则（本地开发辅助，不追踪）
# - 新增 ecosystem/ 肯定规则
# - 新增 sdk/ 肯定规则
# - 更新项目模块结构说明
#
# 项目模块结构（2026-06-15 V12.0 当前）：
# 核心源码 (agentos/) — 仅含源码，构建产物严禁在此
#   - atoms/       : 微内核原子组件层（corekern/coreloopthree/memory/syscall/taskflow/frameworks）
#   - commons/     : 统一基础库（平台抽象/日志/配置/内存/同步等 20+ 子模块）
#   - cupolas/     : 安全穹顶层（防护/清洗/权限/审计/守卫框架）
#   - daemon/      : 用户态守护进程层（gateway_d/llm_d/tool_d/sched_d 等 10+ 服务）
#   - gateway/     : 协议网关层（HTTP/WS/Stdio → JSON-RPC 2.0）
#   - heapstore/   : 运行时数据存储（SQLite + 内存后端混合存储）
#   - protocols/   : 统一协议栈（五层架构）
#
# 生态层 (ecosystem/) — 应用/配置/Prompt/Hook/技能
#   - openlab/     : 开放生态系统（Apps/Contrib/Markets/Core）
#   - manager/     : 统一配置管理中心（多模块 Schema + 热重载）
#   - prompts/     : Prompt 模板体系（cognition/memory/security/system）
#   - hooks/       : Hook 系统（审计/成本/注入/安全）
#   - skills/      : 技能注册目录
#
# SDK 层 (sdk/) — 多语言 SDK
#   - python/      : Python SDK
#   - go/          : Go SDK
#   - rust/        : Rust SDK
#   - typescript/  : TypeScript SDK
#
# 部署 (deploy/) — Docker 容器化部署
#
# 脚本工具集 (scripts/) — CI/CD、开发环境、运维部署、资源管理
#   - ci/           : 持续集成（pipeline/quality/release/verify）
#   - dev/          : 开发环境（build/setup/cli/cmake/config/utils）
#   - ops/          : 运维部署（deploy/benchmark/demo/lib/tests）
#   - resources/    : 项目资源（images/tutorial）
#   - toolkit/      : Python 运维工具包
#
# 测试套件 (tests/) — 单元/集成/契约/基准/安全测试
#
# 项目根目录配置文件
#   - .clang-format / .clang-tidy / .clangd : C/C++ 格式化与诊断
#   - .editorconfig                        : 跨IDE基础规范
#   - configs/                             : 集中配置目录（agentos.yaml + env.example）
#   - .gitattributes                       : Git 换行符处理
#   - .pre-commit-config.yaml             : Pre-commit 钩子
#   - vcpkg.json                           : C++ 依赖清单
#   - Doxyfile                             : API 文档生成
# =============================================================================

# =============================================================================
# 本地文档（不追踪，仅保留在本地）
# =============================================================================
CHANGELOG.md

# =============================================================================
# 跨团队合约文件（本地开发辅助，不追踪）
# contracts/ 存放跨团队接口合约草稿，属于本地开发辅助文件，不纳入版本控制
# =============================================================================
contracts/

# =============================================================================
# CMake 构建系统（构建产物，不追踪）
# =============================================================================

# IDE 配置
.clangd/
.compile_flags.txt

# .clangd 使用相对路径，可以追踪（方便新开发者开箱即用）
!.clangd

# CMake缓存和临时文件（所有层级）
**/CMakeCache.txt
**/CMakeFiles/
**/cmake_install.cmake
**/cmake_install.*
**/CTestTestfile.cmake

# CMake用户配置
CMakeUserPresets.json
CMakePresets.json

# CMake生成的Makefile（递归排除所有子目录）
**/Makefile

# CMake自动生成的版本配置文件
**/AgentRTConfigVersion.cmake

# =============================================================================
# 构建输出目录（所有层级，关键规则！）
# =============================================================================

# 根目录构建目录
build/
_build/
build_test/
build-test/
build_clean/
build_warn/
build_test_protocol/
build_ci*/

# 子目录中的构建产物（防止源内构建）
**/build/
!scripts/dev/build/
**/_build/
**/build-test/
agentos/build/
agentos/*/build/
agentos/*/*/build/

# IDE构建目录
cmake-build*/
cmake-build-debug/
cmake-build-release/
cmake-build-coverage/

# 外部构建目录（Out-of-Source Build）
AgentRT-build/
AgentRT-build-*/
ci-artifacts/

# CMake 构建输出目录（库和可执行文件）
agentos/lib/
agentos/bin/

# 编译器数据库（IDE 工具生成）
compile_commands.json
compile_flags.txt
compile_commands/*.json

# =============================================================================
# 编译产物（构建时生成，不追踪）
# =============================================================================

# 目标文件
*.o
*.obj
*.so
*.so.*
*.dll
*.dll.a
*.dylib
*.lib
*.a
*.la
*.lai
*.d

# 可执行文件
*.exe
*.exe.manifest
*.out
*.app

# 预编译头
*.pch
*.gch
*.pch.gch

# =============================================================================
# Python 相关（运行时生成，不追踪）
# =============================================================================

# Python 缓存
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.pyd
.Python
*.pyc
*.pyc.*

# 虚拟环境
venv/
env/
ENV/
.venv/
.python-version
.venv.bak/

# Python 包管理产物
*.egg-info/
*.egg
*.dist-info/
*.pydist-info/
dist/
wheels/
*.whl

# Poetry
.poetry/
poetry.lock

# Pipenv
Pipfile.lock

# 测试和覆盖率
.pytest_cache/
.coverage
.coverage.*
coverage/
htmlcov/
coverage.xml
.tox/
.nox/

# Jupyter
.ipynb_checkpoints/
profile_default/
ipython_config.py
ipython_kernel_config.py

# 类型检查缓存
.mypy_cache/
.dmypy.json
dmypy.json
.ruff_cache/

# =============================================================================
# Go 语言 SDK（构建产物，不追踪）
# =============================================================================

sdk/go/vendor/
sdk/go/go.sum
sdk/go/*.exe
sdk/go/testsuite/
sdk/go/coverage/
sdk/go/*.test
cover.out
cover.html

# =============================================================================
# Rust 语言 SDK（构建产物，不追踪）
# =============================================================================

sdk/rust/target/
Cargo.lock
sdk/rust/debug/
sdk/rust/release/
sdk/rust/doc/
sdk/rust/target/doc/

# =============================================================================
# TypeScript/JavaScript SDK（构建产物，不追踪）
# =============================================================================

sdk/typescript/node_modules/
sdk/typescript/dist/
sdk/typescript/build/
sdk/typescript/coverage/
sdk/typescript/.tsbuildinfo
sdk/typescript/.pnpm-store/
sdk/typescript/tsbuildinfo/
.next/
.nuxt/

# =============================================================================
# 环境变量和敏感配置（本地配置，不追踪）
# =============================================================================

# 环境文件
*.env
.env.local
.env.*.local
.env.dev
.env.test
.env.production

# 密钥和证书
*.pem
*.key
*.crt
*.cer
*.der
*.p12
*.pfx
*.jks
*.keystore
secrets.json
secrets.yaml
secrets.yml
credentials.json
service_account.json
oauth/
.token

# 本地配置覆盖
manager.local.yaml
manager.local.yml
manager.*.local.yaml
manager.*.local.yml
settings.local.py
local_settings.py
override.yaml

# =============================================================================
# IDE 和编辑器配置（本地开发配置，不追踪）
# =============================================================================

# Visual Studio Code
.vscode/
.trae/
*.code-workspace
*.code-snippets
.vscode-test/

# JetBrains 系列
.idea/
*.iml
*.ipr
*.iws
out/
*.hprof
*.heap
*.clion/

# Vim
*.swp
*.swo
*~
.netrwhist
*.un~
.\#*
*.elc

# Sublime Text
*.sublime-project
*.sublime-workspace

# Xcode
*.xcodeproj/
*.xcworkspace
*.xcuserstated
*.xccheckout
*.pbxuser
*.perspectivev3

# Visual Studio
*.suo
*.user
*.userosscache
.sln.docstates
[Dd]ebug/
[Rr]elease/
!scripts/ci/release/
x64/
x86/
bld/
[Bb]in/
[Oo]bj/

# Eclipse
.classpath
.project
.settings/

# =============================================================================
# 日志、数据库和运行时文件（运行时生成，不追踪）
# =============================================================================

# 日志文件
*.log
logs/
*.logs
agentos/heapstore/logs/

# 数据库文件
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3
*.db.sqlite
*.db-journal
agentos/heapstore/registry/*.db
agentos/heapstore/registry/*.sqlite
agentos/heapstore/db/
registry.db
registry.sqlite

# 追踪和性能数据
agentos/heapstore/traces/
*.trace
*.spans
*.prof
*.profraw
*.gcda
*.gcno

# 缓存
.cache/
*.cache
tmp/
temp/
*.bak
*.backup
*.orig

# =============================================================================
# agentos/heapstore/ 运行时目录（运行时生成，仅保留空路径）
# =============================================================================

# kernel/ 子目录（排除内容，保留 .gitkeep）
agentos/heapstore/kernel/ipc/binder/*
!agentos/heapstore/kernel/ipc/binder/.gitkeep
agentos/heapstore/kernel/ipc/channels/*
!agentos/heapstore/kernel/ipc/channels/.gitkeep
agentos/heapstore/kernel/memory/index/*
!agentos/heapstore/kernel/memory/index/.gitkeep
agentos/heapstore/kernel/memory/meta/*
!agentos/heapstore/kernel/memory/meta/.gitkeep
agentos/heapstore/kernel/memory/raw/*
!agentos/heapstore/kernel/memory/raw/.gitkeep
agentos/heapstore/kernel/memory/patterns/*
!agentos/heapstore/kernel/memory/patterns/.gitkeep

# services/ 子目录（排除内容，保留 .gitkeep）
agentos/heapstore/services/llm_d/*
!agentos/heapstore/services/llm_d/.gitkeep
agentos/heapstore/services/market_d/*
!agentos/heapstore/services/market_d/.gitkeep
agentos/heapstore/services/tool_d/*
!agentos/heapstore/services/tool_d/.gitkeep

# models/ 子目录（排除内容，保留 .gitkeep）
agentos/heapstore/models/*
!agentos/heapstore/models/.gitkeep

# 其他运行时目录
agentos/heapstore/logs/*
agentos/heapstore/traces/*
agentos/heapstore/registry/*
agentos/heapstore/db/*
!agentos/heapstore/logs/.gitkeep
!agentos/heapstore/traces/.gitkeep
!agentos/heapstore/registry/.gitkeep
!agentos/heapstore/db/.gitkeep

# =============================================================================
# 测试相关（测试结果不追踪，测试代码保留）
# =============================================================================

# 测试结果和报告
test-results/
test_output/
*.spec
*.snapshot

# 测试覆盖率
*.gcov

# 测试数据（运行时生成）
test_data/generated/
*.test.data
tests/fuzz/*.fuzz
tests/fuzz/corpus/
tests/fuzz/seeds/
tests/fuzz/hangs/

# 基准测试结果
tests/benchmarks/**/results/
tests/benchmarks/**/plots/
tests/benchmarks/**/reports/
*.benchmark
benchmark_results/
perf_results/

# tests/benchmarks/concurrency/report/（排除内容，保留 .gitkeep）
tests/benchmarks/concurrency/report/*
!tests/benchmarks/concurrency/report/.gitkeep

# =============================================================================
# 文档生成（构建产物，不追踪）
# =============================================================================

doxygen/
xml/
html/
latex/
man/
site/
toolkit/go/doc/

# =============================================================================
# 操作系统文件（系统生成，不追踪）
# =============================================================================

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icn
.apdisk

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
@eaDir/
*.lnk
*.url

# Linux
.fuse_hidden*
.Trash-*
.nfs*

# =============================================================================
# 依赖管理（本地安装，不追踪）
# =============================================================================

node_modules/
**/node_modules/
vendor/
vendor/bundle/
Gemfile.lock
composer.lock
Carthage/Build/
.swiftpm/

# =============================================================================
# 大型文件和二进制文件（运行时生成，不追踪）
# =============================================================================

# FAISS 索引
agentos/heapstore/models/*.faiss
agentos/heapstore/models/*.index
agentos/heapstore/models/*.pkl
agentos/heapstore/models/*.pickle

# 模型权重
agentos/heapstore/model/
agentos/heapstore/models/*.bin
agentos/heapstore/models/*.pth
agentos/heapstore/models/*.h5
agentos/heapstore/models/*.pt
agentos/heapstore/models/*.onnx
*.tflite

# 向量和嵌入
*.npy
*.npz
*.parquet
*.arrow
*.feather

# 数据集（仅排除根目录数据目录）
/data/
/datasets/

# 压缩包（部署目录下的）
deploy/docker/**/*.tar.gz
deploy/docker/**/*.tar.bz2
deploy/docker/**/*.tar.xz
deploy/docker/**/*.zip
deploy/docker/**/*.rar
deploy/docker/**/*.7z

# Docker 覆盖和集群配置
docker-compose.override.yml
.docker/
*.kubeconfig
.kube/

# =============================================================================
# 部署和安装（构建产物，不追踪）
# =============================================================================

deploy/docker/artifacts/
deploy/docker/packages/
install/
installed/
VERSION
version.txt
build_version.txt

# =============================================================================
# 代码质量工具缓存（运行时生成，不追踪）
# =============================================================================

.pre-commit-manager.local.yaml
.eslintcache
.prettierrc.local
.prettierignore.local
.flake8
*.flake8
.pylintrc.local
.pylint.d/
cppcheck.local.xml
.semgrep.local.yaml
.lizard.out
.jscpd/
.jscpd-store/
.scannerwork/
sonar-report.json
.cache/clangd/

# =============================================================================
# 本地开发文档（临时文件，不追踪）
# =============================================================================

*.draft
*.todo
*.notes
DRAFT*
NOTES*
TODO*
CHANGELOG.local.md
CHANGELOG.local
*.md.bak
*.md.backup
README.local.md
README.local
*.tmp
*.tmp.sh
*.tmp.bat
*.tmp.ps1
temp_*
tmp_*
*.personal
*.private
*.local
settings.local
override.local
docs/local/
docs/private/
wiki/
agentos/heapstore/reports/
reports/
test_reports/
coverage_reports/

# 本地生成的分析/检查报告（非项目必需）
*检查报告*.md
*验证报告*.md
*分析报告*.md
*评估报告*.md
*穷尽检查*.md

# =============================================================================
# 必须保留的例外（项目必需文件）
# =============================================================================

# 环境模板
!configs/env.example
!configs/agentos.yaml

# =============================================================================
# 根目录配置文件
# =============================================================================

!.clang-format
!.clang-tidy
!.clangd
!.editorconfig
!.gitattributes
!.pre-commit-config.yaml
!vcpkg.json
!Doxyfile

# ecosystem/manager/ 配置文件
!ecosystem/manager/**/*.yaml
!ecosystem/manager/**/*.yml
!ecosystem/manager/**/*.template
!ecosystem/manager/**/*.schema.json
!ecosystem/manager/.env.template

# 关键文档
!BUILD.md
!README.md
!README_zh.md
!CONTRIBUTING.md
!AUTHORS.md
!SECURITY.md
!SUPPORT.md
!CODE_OF_CONDUCT.md
!NOTICE
!ACKNOWLEDGMENTS.md

# =============================================================================
# scripts/ 脚本文件 — 对齐 V11.0 实际目录结构（ci/dev/ops/resources/toolkit）
# =============================================================================

# scripts/ 通用脚本（所有子目录）
!scripts/**/*.sh
!scripts/**/*.py
!scripts/**/*.bat
!scripts/**/*.ps1
!scripts/**/*.yml
!scripts/**/*.yaml

# scripts/ci/ — 持续集成
!scripts/ci/**/*.sh
!scripts/ci/**/*.py
!scripts/ci/**/*.yaml
!scripts/ci/**/*.yml
!scripts/ci/**/CMakeLists.txt

# scripts/dev/ — 开发环境
!scripts/dev/**/*.sh
!scripts/dev/**/*.py
!scripts/dev/**/*.bat
!scripts/dev/**/*.ps1
!scripts/dev/**/*.yaml
!scripts/dev/**/*.yml
!scripts/dev/cmake/*.h

# scripts/ops/ — 运维部署
!scripts/ops/**/*.sh
!scripts/ops/**/*.py
!scripts/ops/**/*.yaml
!scripts/ops/**/*.yml
!deploy/docker/k8s/**/*.yml
!deploy/docker/k8s/**/*.yaml

# scripts/resources/ — 项目资源
!scripts/resources/**/*.sh
!scripts/resources/**/*.py
!scripts/resources/**/*.json
!scripts/resources/**/*.yaml
!scripts/resources/**/*.yml
!scripts/resources/images/*.{svg,png,jpg,gif}
!scripts/resources/tutorial/**/*

# scripts/toolkit/ — 运维工具包
!scripts/toolkit/**/*.sh
!scripts/toolkit/**/*.py
!scripts/toolkit/**/*.yaml
!scripts/toolkit/**/*.yml

# 手写 Makefile（非 CMake 生成，需保留）
!tests/Makefile

# =============================================================================
# tests/ 测试目录
# =============================================================================

!tests/**/*.py
!tests/**/*.sh
!tests/**/*.yaml
!tests/**/*.yml
!tests/**/*.c
!tests/**/*.h
!tests/**/*.go
!tests/**/*.rs
!tests/.coveragerc
!tests/.gitkeep
!tests/benchmarks/**/*.gitkeep
!tests/benchmarks/concurrency/report/.gitkeep
!tests/unit/sdk/python/.gitkeep
!tests/unit/sdk/rust/.gitkeep
!tests/unit/services/llm_d/.gitkeep
!tests/unit/services/tool_d/.gitkeep

# =============================================================================
# Docker 和部署配置
# =============================================================================

!Dockerfile*
!docker-compose*.yml
!docker-compose*.yaml
!ecosystem/openlab/Dockerfile
!deploy/docker/Dockerfile*
!agentos/daemon/Dockerfile.ci

# =============================================================================
# CMake 构建配置
# =============================================================================

!agentos/atoms/**/CMakeLists.txt
!agentos/commons/**/CMakeLists.txt
!agentos/commons/tests/CMakeLists.txt
!agentos/cupolas/CMakeLists.txt
!agentos/daemon/**/CMakeLists.txt
!agentos/gateway/CMakeLists.txt
!agentos/heapstore/**/CMakeLists.txt
!tests/unit/**/CMakeLists.txt

# 根目录项目配置
!pyproject.toml

# =============================================================================
# ecosystem/ 生态层配置文件
# =============================================================================

!ecosystem/**/*.yaml
!ecosystem/**/*.yml
!ecosystem/**/*.json
!ecosystem/**/*.py
!ecosystem/**/*.sh
!ecosystem/**/*.j2
!ecosystem/**/*.md

# =============================================================================
# deploy/ 部署配置
# =============================================================================

!deploy/**/*.yml
!deploy/**/*.yaml
!deploy/**/*.json
!deploy/**/Dockerfile*
!deploy/**/*.md
!deploy/**/.env.example

# =============================================================================
# sdk/ 语言 SDK 配置
# =============================================================================

!sdk/python/pyproject.toml
!sdk/python/setup.py
!sdk/go/go.mod
!sdk/rust/Cargo.toml
!sdk/typescript/package.json
!sdk/typescript/tsconfig.json
!sdk/typescript/jest.config.js

# =============================================================================
# 其他必须保留的文件
# =============================================================================

!ecosystem/openlab/pyproject.toml
!ecosystem/openlab/requirements.txt
!agentos/commons/utils/types/.gitkeep
!LICENSE
!NOTICE
!.github/CODEOWNERS

# =============================================================================
# GitHub 平台模板目录（项目必需，保留）
# =============================================================================
# .github/ 是 GitHub 平台配置目录，随仓库同步
# 包含 CI/CD 工作流、Issue 模板、PR 模板等

# =============================================================================
# 强化防护规则（V11.0 更新）
# =============================================================================

# 防止意外追踪非源码文件（白名单模式）
# 只有以下类型的文件应该被 git 追踪：
# - 源码: *.c, *.h, *.py, *.ts, *.tsx, *.rs, *.go, *.js
# - 配置: CMakeLists.txt, *.yml, *.yaml, *.json, *.toml
# - 文档: *.md (仅项目必需文档)
# - 脚本: *.sh, *.bat, *.ps1
# - 其他: Dockerfile*, .gitignore, .gitkeep, LICENSE, NOTICE

# 本地生成的临时脚本（不追踪）
*.local.sh
*.local.py
*.local.bat
*debug*.sh
*test-local*.sh
*quick-test*.sh

# IDE/编辑器工作区文件（强化排除）
*.workspace
*.code-workspace
.vscode/*.json
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json

# 操作系统元数据（强化排除）
.fuse_hidden*
.nfs*
.Trashes*
.apdisk
._*

# =============================================================================
# 结束
# =============================================================================
#
# V12.0.0 维护说明：
# 1. scripts/ 子目录变更时需同步更新 scripts/ 肯定规则节
# 2. agentos/ 新增模块时需在 CMake 配置节添加否定规则
# 3. 根目录新增配置文件时需在根目录配置节添加否定规则
# 4. 添加新规则时，必须验证不会误排除项目必需文件
# 5. 使用 git check-ignore -v <file> 验证规则生效
# 6. 定期运行 git status --porcelain 检查是否有意外追踪的文件
# 7. 项目必需文件必须在肯定规则节有明确的 ! 前缀规则
# 8. agentos/ 路径仅含源码，构建产物输出至 AgentRT-build/
# 9. ecosystem/ 包含 openlab/manager/prompts/hooks/skills，配置文件需保留
# 10. sdk/ 包含四语言 SDK，各语言构建产物需排除但配置文件需保留
# 11. contracts/ 本地开发辅助文件，不追踪（.h/.c 合约草稿）
