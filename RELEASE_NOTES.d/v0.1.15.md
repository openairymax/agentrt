- **社区反馈六类问题修复（安装/更新/启动/稳定/长对话/长任务）**：安装器补齐 `detect_existing_install()` 定义；修复 Debian 系加载器上 aarch64 的架构误判，改以 userland 位宽为权威判据；发布渠道白名单三副本收敛为 `stable|rc|beta`，RISC-V 撤出预编译白名单并明示源码构建指引；CLI 对话主路径统一经网关 `llm.complete`；对话历史引入环形窗口裁剪，reasoning 累计封顶并按轮转写日志；网关等待可取消、超时可一跳诊断；默认 REPL 直接显示拼音候选栏。
- **核心运行稳定性**：corekern 内核经 `airy_init()` 正式接入 CLI 启动链路，并输出可追溯的启动证据；修复 IPC 通道、IPC 回复路由、binder 关闭顺序与零长度分配等一批崩溃/泄漏缺陷。
- **终端体验**：TUI 在收到致命信号时恢复终端状态（不再残留乱码或隐藏光标）；正确消费终端 OSC 回复并新增标题分隔线；背景色解析零初始化，避免随机色值；按键刷新改用 `AIRY_MEMCPY_SAFE`，消除未定义行为。
- **安全加固**：网关入口鉴权与监听绑定加固；commons、gateway、corekern 引入 ASan/UBSan 消毒器质量门禁，内存安全缺陷在合并前拦截。
- **构建与诊断**：新增 `AIRY_KEEP_SYMBOLS` 开关，发布包可按需保留符号用于现场诊断。
- **发布工程**：九项质量门禁在发布平面统一执行；Windows 两条构建腿串行化，规避并发资源争用导致的构建抖动；新增 9.8 发布门禁，组 A~K 共 29 项断言入 ctest `release_gates`（linux+macos required），CI sdk 取料 fail-closed。
